{
  "name": "tweedeo-scrapper",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "make": "rm -rf make && npx ts-node tasks/make.ts",
    "test": "jest --passWithNoTests",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "husky": "^8.0.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
